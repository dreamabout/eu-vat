services:
  app:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    # Keep the container alive so `docker compose exec app <cmd>` works the same
    # way it does in kaikei — one command idiom across both repos.
    command: ["tail", "-f", "/dev/null"]
