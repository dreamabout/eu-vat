<?php

declare(strict_types=1);

namespace Kaikei\EuVat\Exception;

/** Marker for every exception this package throws, so consumers can catch one type. */
interface EuVatException extends \Throwable
{
}
